import { TwinkleDirective } from './twinkle.directive';

describe('TwinkleDirective', () => {
  it('should create an instance', () => {
    const directive = new TwinkleDirective();
    expect(directive).toBeTruthy();
  });
});
